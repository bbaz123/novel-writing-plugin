# 更新记录

本仓库是 [novel-studio](https://github.com/bbaz123/novel-studio) 内置创作插件的**发布镜像**，
版本号跟随 novel-studio 的发布节奏；插件的自身版本号以 [`plugin.json`](plugin.json) 的 `version` 字段为准。

## v0.9.3 — 设定轻量装配 + headless 瘦身

- **`novel_context` 新增 `settings` 模式**：设定类生成（世界观 / 角色卡 / 大纲 / 长期记忆等）只去掉
  「当前场景 / 本章蓝图 / 前后章衔接」三层，质量层（红线、角色卡、世界观词条、长期记忆、事件账本、
  未闭合伏笔）零丢失，省下的上下文留给真正要产出的内容。
- **headless profile 瘦身**：`headless-cordis.patch.yml` 关闭与创作无关的通用能力——
  `agent-instructions`（省 ~4.1k）、`tool-pwsh`（最大的单个工具 schema）、
  `workflow` / `subagent` / `subagent-fork` / `subagent-control` / `subagent-list-agents`、
  `todo` / `goal` / `jobs` / `ralph`、`plan-mode`、`web`、`skill` / `skill-filesystem`、`session-title-llm`；
  `novel_*` 工具与 read / write / edit / glob / grep、persona、OpenViking 记忆插件全部保留。
- **冒烟测试扩至 32 组**：新增 `settings` 模式轻量装配断言（验证质量层不丢失）。

## v0.8.0 — 上下文质量与性能

- **评分制出场角色**：出场角色不再「按名字前 8 兜底」，改为评分制选择——剧情线关联 > 正文/摘要命中次数
  > 蓝图·作者注·最近事件提及 > 最近章节摘要出场 > 人物关系网；上限 16，兜底按最近出场优先。
- **别名与整词命中**：角色卡新增「别名/称呼」字段，上下文与一致性核对都按正式名 + 别名命中；
  单字 CJK 名称要求词边界，杜绝「云」命中「云彩/李云」类子串误报。
- **角色卡核心保底**：出场角色卡层不再整层头部盲截，改为逐卡截断 + 每卡名字/身份/性格/当前状态必保，
  长字段分级压缩。
- **上下文预览页签**：写作页参考面板新增「上下文」页签——预览 AI 实际收到的分层装配与出场角色名单，
  可勾选角色强制带入本章（章节级覆盖）。
- **角色状态闭环**：`novel_consistency` 为每个出场角色附带相关最近事件；AI 用
  `novel_event_add(kind="character")` 记录状态变化，作者在角色面板一键同步为当前状态。
- **上下文缓存与性能**：`/api/novel/context` 装配结果内存缓存（任何写操作自动失效）；
  长章节只按需转换头部/尾部纯文本，不再整章全文剥标签；dsh 工具 GET 连接失败自动重试一次。
- **OpenViking 语义召回层**：新增语义召回上下文层，可按需禁用并降级。

## v0.7.0 — 插件体检补强

- 红线管理 UI、**整词豁免词**、**正向风格契约**（作品级正向写作要求随红线一起进入上下文）。
- 长期记忆**版本界面**：历史版本列表、一键回滚、与当前摘要的差异预览。
- 伏笔状态工具 `novel_foreshadow_update`（resolved / dropped / open，可回链回收事件）。

## v0.6.0 — 审稿修稿闭环与创作面板

- **审稿 → 修稿闭环**：审稿报告 → 逐条确认/忽略 → 按确认清单修稿 → 段落级差异预览 → 合并到正文。
- **伏笔/叙事线索面板**：分组展示、跳转埋设章节、标记回收/废弃/恢复。
- **批量章节生成**：从第一个无正文章节顺序生成 N 章（≤10），可随时停止，失败即停。
- **导入导出**：TXT / Markdown / EPUB 导入（自动拆章），整书 TXT/Markdown 与单章 TXT 导出。

## v0.5.0 — 章节蓝图与目标字数

- **章节蓝图**：写前先出蓝图（场景目标 / 情节点 / 冲突与转折 / 角色状态变化 / 钩子 / 参考设定），
  作者确认后落库，随上下文带入并作为一致性核对的锚点；生成失败自动降级为直接成文，不阻塞。
- **每章目标字数**：作品级默认（默认 2000，可 3000/5000/自定义）+ 章节级覆盖；成文不足时自动续写补足（≤2 轮）。
- **多关键词加权检索**：`/api/search` 支持多关键词 AND 匹配、名称/标题加权排序、片段定位。

## v0.4.0 — 篇幅纪律

- 整章成文篇幅纪律：默认 2000 字以上，拒绝千字残章。

## v0.1.0 — 初版创作内核

- 分层上下文装配（ST 式）、反 AI 腔红线扫描、事件账本与伏笔闭环、长期记忆版本化、
  提案确认（headless 防污染）、13 个 `novel_*` 工具与写作人设 preset。
